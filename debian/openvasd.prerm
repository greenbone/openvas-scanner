#!/bin/sh

if [ "$1" != "upgrade" ]; then
	if [ -x "/etc/init.d/openvasd" ] ; then
	        /etc/init.d/openvasd stop
	else
                start-stop-daemon --stop --quiet --oknodo --exec /usr/sbin/openvasd
	fi
fi


#DEBHELPER#


exit 0
