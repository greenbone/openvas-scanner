#!/bin/sh

if [ "$1" != "upgrade" ]; then
	if [ -x "/etc/init.d/nessusd" ] ; then
	        /etc/init.d/nessusd stop
	else
                start-stop-daemon --stop --quiet --oknodo --exec /usr/sbin/nessusd
	fi
fi


#DEBHELPER#


exit 0
